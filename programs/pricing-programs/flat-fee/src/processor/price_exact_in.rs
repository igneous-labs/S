use flat_fee_interface::PriceExactInIxArgs;
use solana_program::{account_info::AccountInfo, entrypoint::ProgramResult};

pub fn process_price_exact_in(
    _accounts: &[AccountInfo],
    _args: PriceExactInIxArgs,
) -> ProgramResult {
    todo!()
}
